<ResourceDictionary 
    x:Class="CryptoMessenger.Views.Styles.ButtonStyle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CryptoMessenger.Views.Styles"
    xmlns:props="clr-namespace:CryptoMessenger.Properties"
    >

    <!-- button style -->
    <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <!-- background color -->
                        <Rectangle 
                            x:Name="BackgroundRect"
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch" 
                            Fill="{Binding Path=ButtonBackgroundNormalBrush, Source={x:Static props:Settings.Default}}"
                            />
                        <!-- background image -->
                        <Rectangle 
                            x:Name="buttonBackground"
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch" 
                            Fill="{TemplateBinding Foreground}"
                            OpacityMask="{TemplateBinding Background}"
                            />
                        <!-- text -->
                        <ContentPresenter 
                            x:Name="contentPresenter"
                            Margin="10,0,10,0"
                            TextOptions.TextRenderingMode="ClearType"
                            TextOptions.TextFormattingMode="Display"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            TextBlock.Foreground="{TemplateBinding Foreground}"
                            Content="{TemplateBinding Content}" 
                            />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundRect" Property="Fill" 
                                    Value="{Binding Path=ButtonBackgroundHoverBrush, Source={x:Static props:Settings.Default}}"
                                    />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BackgroundRect" Property="Fill" 
                                    Value="{Binding Path=ButtonBackgroundNormalBrush, Source={x:Static props:Settings.Default}}"
                                    />
                            <Setter TargetName="buttonBackground" Property="Fill" 
                                    Value="{Binding Path=ButtonForegroundPressedBrush, Source={x:Static props:Settings.Default}}"
                                    />
                            <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" 
                                    Value="{Binding Path=ButtonForegroundPressedBrush, Source={x:Static props:Settings.Default}}"
                                    />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="buttonBackground" Property="Fill" Value="LightGray"/>
                            <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" Value="LightGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
    