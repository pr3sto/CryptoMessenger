<ResourceDictionary 
    x:Class="CryptoMessenger.Views.Styles.ButtonStyle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CryptoMessenger.Views.Styles"
    >

    <!-- brushes -->
    <SolidColorBrush x:Key="ButtonBackgroundNormalBrush" Color="DarkGray"/>
    <SolidColorBrush x:Key="ButtonBackgroundHoverBrush" Color="DarkGray"/>
    <SolidColorBrush x:Key="ButtonForegroundPressedBrush" Color="White"/>

    <!-- button style -->
    <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <!-- background color -->
                        <Rectangle 
                            x:Name="BackgroundRect"
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch" 
                            Fill="{DynamicResource ButtonBackgroundNormalBrush}"
                            />
                        <!-- background image -->
                        <Rectangle 
                            x:Name="buttonBackground"
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch" 
                            Fill="{TemplateBinding Foreground}"
                            OpacityMask="{TemplateBinding Background}"
                            />
                        <!-- text -->
                        <ContentPresenter 
                            x:Name="contentPresenter"
                            Margin="10,0,10,0"
                            TextOptions.TextRenderingMode="ClearType"
                            TextOptions.TextFormattingMode="Display"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            TextBlock.Foreground="{TemplateBinding Foreground}"
                            Content="{TemplateBinding Content}" 
                            />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundRect" Property="Fill" 
                                    Value="{DynamicResource ButtonBackgroundHoverBrush}"
                                    />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BackgroundRect" Property="Fill" 
                                    Value="{DynamicResource ButtonBackgroundNormalBrush}"
                                    />
                            <Setter TargetName="buttonBackground" Property="Fill" 
                                    Value="{DynamicResource ButtonForegroundPressedBrush}"
                                    />
                            <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" 
                                    Value="{DynamicResource ButtonForegroundPressedBrush}"
                                    />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
    