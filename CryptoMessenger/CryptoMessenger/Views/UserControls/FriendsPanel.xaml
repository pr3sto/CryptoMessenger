<UserControl 
    x:Class="CryptoMessenger.Views.UserControls.FriendsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    xmlns:local="clr-namespace:CryptoMessenger.Views.UserControls"
    xmlns:props="clr-namespace:CryptoMessenger.Properties"    
    >
    <Grid Height="460">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!-- empty list notify -->
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            TextOptions.TextRenderingMode="ClearType"
            TextOptions.TextFormattingMode="Display"
            FontSize="26" 
            Foreground="{Binding Path=SpecialBrush, Source={x:Static props:Settings.Default}}"
            Text="{Binding Source={x:Static props:Resources.EMPTYFRIENDS_LISTBOX_PLACEHOLDER}}">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=friendsListbox, Path=Items.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <!-- list of friends -->
        <ListBox
            x:Name="friendsListbox" 
            Grid.Row="2"
            Grid.Column="0"
            Style="{StaticResource ListBoxStyle}"
            ItemContainerStyle="{StaticResource ListBoxItemStyle}"
            ItemsSource="{Binding FriendsList}"
            SelectedValue="{Binding SelectedFriend}"
            Margin="20,10,10,20"  
            HorizontalContentAlignment="Stretch"
            >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <!-- friend's login -->
                        <TextBlock
                            x:Name="textBlock"
                            Height="25"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            TextOptions.TextRenderingMode="ClearType"
                            TextOptions.TextFormattingMode="Display"
                            FontSize="17"
                            Foreground="{Binding Path=SecondarySecondBrush, Source={x:Static props:Settings.Default}}"
                            Text="{Binding Name}"
                            />
                        <!-- remove from friends button -->
                        <Button 
                            x:Name="button"
                            Style="{StaticResource ListBoxButtonStyle}"
                            Cursor="Hand"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Height="25" Width="25"
                            Foreground="White"
                            Command="{Binding Path=RemoveFriendCommand}"
                            ToolTip="{Binding Source={x:Static props:Resources.REMOVEFRIEND_BUTTON_TOOLTIP}}"
                            Visibility="Collapsed"
                            >
                            <Button.Background>
                                <ImageBrush ImageSource="/CryptoMessenger;component/Resources/cross.png"/>
                            </Button.Background>
                        </Button>
                    </Grid>

                    <DataTemplate.Triggers>
                        <!-- on mouse hover -->
                        <DataTrigger
                            Binding="{Binding
                                RelativeSource={RelativeSource Mode=FindAncestor,
                                AncestorType={x:Type ListBoxItem}}, Path=IsMouseOver}"
                            Value="True"
                            >
                            <Setter TargetName="textBlock" Property="Foreground" 
                                    Value="{Binding Path=SecondaryFirstBrush, Source={x:Static props:Settings.Default}}"/>
                            <Setter TargetName="button" Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <!-- on selected item -->
                        <DataTrigger
                            Binding="{Binding
                                RelativeSource={RelativeSource Mode=FindAncestor,
                                AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                            Value="True"
                            >
                            <Setter TargetName="textBlock" Property="Foreground" 
                                    Value="{Binding Path=SpecialBrush, Source={x:Static props:Settings.Default}}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- select friend notify -->
        <TextBlock  
            Grid.Column="1"
            Grid.Row="0"
            Grid.RowSpan="2"
            Width="200"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            TextOptions.TextRenderingMode="ClearType"
            TextOptions.TextFormattingMode="Display"
            FontSize="26" 
            Foreground="{Binding Path=SpecialBrush, Source={x:Static props:Settings.Default}}"
            TextWrapping="Wrap"
            TextAlignment="Center"
            Text="{Binding Source={x:Static props:Resources.SELECTFRIEND_NOTIFICATION}}"
            Visibility="{Binding IsFriendSelected, Converter={StaticResource InvertAndVisibilitate}}"
            />

        <!-- message textbox -->
        <TextBox
            Grid.Column="1"
            Grid.Row="2"
            Style="{StaticResource TextBoxStyle}"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Stretch"
            Height="100"
            Margin="10,0,70,20"
            TextWrapping="Wrap"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"
            TextOptions.TextRenderingMode="ClearType"
            TextOptions.TextFormattingMode="Display"
            FontSize="17"
            Text="{Binding Path=MessageText, UpdateSourceTrigger=PropertyChanged}"
            Tag="{Binding Source={x:Static props:Resources.MESSAGE_TEXTBOX_PLACEHOLDER}}"
            Visibility="{Binding IsFriendSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
            />
        <!-- send message button -->
        <Button
            Grid.Column="1"
            Grid.Row="2"
            Style="{StaticResource ButtonStyle}"
            Cursor="Hand"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right"
            Margin="0,0,20,20"
            Height="100" Width="50"
            Foreground="White"
            Command="{Binding Path=SendCommand}"
            ToolTip="{Binding Source={x:Static props:Resources.SENDMESSAGE_BUTTON_TOOLTIP}}"
            Visibility="{Binding IsFriendSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
            IsEnabled="{Binding Path=MessageText, Converter={StaticResource StringEmptynessToBooleanConverter}}"
            >
            <Button.Background>
                <ImageBrush ImageSource="/CryptoMessenger;component/Resources/send.png"/>
            </Button.Background>
        </Button>
        
        <!-- dialog panel -->
        <Border 
            Grid.Column="1"
            Grid.Row="0"
            Grid.RowSpan="2"
            Margin="10,10,20,120"
            RenderOptions.EdgeMode="Aliased"
            BorderBrush="{Binding Path=MainSecondBrush, Source={x:Static props:Settings.Default}}" 
            BorderThickness="2 2 2 0"
            Visibility="{Binding IsFriendSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
            >
            <ListBox
                Style="{StaticResource ListBoxStyle}"
                ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                ItemsSource="{Binding RepliesList}"
                Margin="5,5,5,5"
                Background="{Binding Path=MainFirstBrush, Source={x:Static props:Settings.Default}}"
                HorizontalContentAlignment="Stretch"
                >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border
                            BorderBrush="{Binding Path=MainSecondBrush, Source={x:Static props:Settings.Default}}" 
                            BorderThickness="0 0 0 1"
                            >
                            <Grid Margin="0,0,0,20">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- login -->
                                <TextBlock
                                    Grid.Row="0"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left"
                                    TextOptions.TextRenderingMode="ClearType"
                                    TextOptions.TextFormattingMode="Display"
                                    FontSize="17"
                                    Foreground="{Binding Path=SpecialBrush, Source={x:Static props:Settings.Default}}"
                                    Text="{Binding Author}"
                                    />
                                <!-- time -->
                                <TextBlock
                                    Grid.Row="0"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Right"
                                    TextOptions.TextRenderingMode="ClearType"
                                    TextOptions.TextFormattingMode="Display"
                                    FontSize="17"
                                    Foreground="{Binding Path=SpecialBrush, Source={x:Static props:Settings.Default}}"
                                    Text="{Binding Path=Time, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                    />
                                <!-- message -->
                                <TextBlock
                                    Grid.Row="1"
                                    VerticalAlignment="Bottom"
                                    HorizontalAlignment="Left"
                                    TextOptions.TextRenderingMode="ClearType"
                                    TextOptions.TextFormattingMode="Display"
                                    FontSize="17"
                                    Foreground="{Binding Path=SecondarySecondBrush, Source={x:Static props:Settings.Default}}"
                                    Text="{Binding Text}"
                                    />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

    </Grid>
</UserControl>
